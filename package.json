{
  "name": "healthcheck-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal HTTP service with a /healthz endpoint, packaged as a hardened container image",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
